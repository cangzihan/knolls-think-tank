---
tags:
  - Windows
  - Linux
  - Ubuntu
  - Debian
---

# 通用命令

## Git配置Clash代理
```shell
git config --global http.proxy http://127.0.0.1:7890
git config --global https.proxy https://127.0.0.1:7890
git config --global --unset http.proxy
git config --global --unset https.proxy
```

## RustScan
- 检测本机接口占用`rustscan -a localhost`
- To sscan individual ports: `rustscan -a 127.0.0.1 -p 53`
- To scan a range of ports: `rustscan -a 127.0.0.1 --range 1-1000`
